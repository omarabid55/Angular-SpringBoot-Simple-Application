import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  title = 'Angular & SpringBoot simple Application';
  description = 'Add customers from Angular and save them on DerbyDB using SpringBoot';
}
